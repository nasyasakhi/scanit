import 'package:flutter/material.dart';

// A primary color definition
const primaryColor = Color(0xFFF79F26);
const secondaryColor = Color(0xFF30A441);
const bgColor = Color(0xFFFFF6E7); 

// animationDuration time definition
const animationDuration =
    Duration(milliseconds: 200); // best practicenya 200-400

//default constant value for padding size
const defaultPadding = 20.0;
